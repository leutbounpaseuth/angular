import { CanActivateWhenLoggedIn } from './can-activate-when-logged-in';

describe('CanActivateWhenLoggedIn', () => {
  it('should create an instance', () => {
    expect(new CanActivateWhenLoggedIn()).toBeTruthy();
  });
});
